add_library(messaging_module
    inbox.cpp
)

target_link_libraries(messaging_module
    PRIVATE
        spdlog::spdlog
)

target_include_directories(messaging_module
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}/../../include
)